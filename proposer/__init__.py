"""Fleet track 2: the proposal layer.

This package reads. It proposes. It does not act.

There is no write path to deadly_digital here, no write path to any track 1
table, and no model call. V1's findings are arithmetic over track 1's own
tables, so nothing in this package needs a model to be correct, and adding
one would make an observation that can be checked into an assertion that
cannot.
"""
